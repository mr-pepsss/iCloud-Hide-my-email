Этот скрипт автоматизирует работу с функцией Hide My Email в iCloud+.

Обычно Apple позволяет создавать новые адреса-алиасы только вручную, при этом действует ограничение — не более 5 адресов в час. Скрипт берёт эту задачу на себя:
автоматически открывает iCloud+ и ждёт, пока вы авторизуетесь;
создаёт новые адреса по порядку (401, 402, 403 и так далее);
сохраняет все созданные почтовые адреса в отдельный файл для удобного использования;
отслеживает достижение лимита Apple и автоматически ждёт необходимое время;
показывает обратный отсчёт ожидания, чтобы было видно, сколько осталось ждать;
после паузы обновляет страницу и продолжает работу без вашего участия.

Как пользоваться

Запустите скрипт.
Если вы ещё не авторизованы — войдите в свой iCloud (один раз).
Далее скрипт работает автоматически: создаёт почты, записывает их в файл, ждёт час и продолжает.
Скрипт может работать без вашего участия столько времени, сколько потребуется.
Браузер, в котором запущен скрипт, можно сразу свернуть — это не повлияет на его работу.
📌 Идея простая: вы один раз запускаете процесс — и он самостоятельно создаёт для вас новые iCloud-адреса партиями, строго соблюдая лимиты Apple и не требуя ручного контроля.


Donate EVM 0xFa61eb476981FE9aA9A9c8afA26dc81D45Db5917


**УСТАНОВКА**

1. Создать папку проекта - например icloud-bot
2. Скачать файлы скрипта
3. Подготовить папку проекта
    
    В папке проекта должны лежать:
    
     `main.py` — сам скрипт (мы уже собрали полный вариант).
     `.env` — настройки.
     `aliases.txt` — создастся автоматически, сюда будут записываться номера и почты.

Структура будет такая
icloud-bot/
  main.py
  .env
  aliases.txt  (создастся сам)
  icloud_profile/ (папка профиля, создастся сама)

4. Открываем папку в своем терминале
5. Cоздаём виртуальное окружение

python3 -m venv venv
source venv/bin/activate   # macOS / Linux
# venv\Scripts\activate    # Windows PowerShell

6. Ставим библиотеки
   pip install playwright python-dotenv
   
7. Устанавливаем браузеры для Playwright
   playwright install chromium


**НАСТРОЙКА**

Все настройки осуществляются в файле `.env` 

`main.py` мы вообще не трогаем

В `.env` лежат параметры (уже заполнены дефолтами):

Ниже параметры которые можно/нужно менять


ICLOUD_URL=https://www.icloud.com/icloudplus/
PERSIST_DIR=./icloud_profile
HEADLESS=false # Пока работает только с false, возможно допилю до безголового режима.
BATCH_SIZE=5 # сколько адресов подряд создавать, ограничение icloud 5шт в час
SLEEP_SECONDS=3610 # сколько спать между батчами (3600 = 1 час)
ALIASES_FILE=aliases.txt # куда записывать сгенерированные почты
DEFAULT_TIMEOUT_MS=10000 
WAIT_AFTER_CLICK_MS=400
START_NUMBER=1 # с какого номера начинать метку для адреса

# Юзер агент для браузера который мы поднимаем 
# Возьмите эту часть и попросите chatGPT сгенерировать вам новый юзерагент, чтобы мы не пользовальсь все одним и тем же.
FIXED_UA=Mozilla/5.0 (Macintosh; Intel Mac OS X 14_1) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.6099.129 Safari/537.36
LOCALE=ru-RU
TIMEZONE=Europe/Warsaw


# ВСЕ ЧТО НИЖЕ МЫ НЕ ТРОГАЕМ


Donate EVM 0xFa61eb476981FE9aA9A9c8afA26dc81D45Db5917
