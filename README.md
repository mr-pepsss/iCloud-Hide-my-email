Этот скрипт автоматизирует работу с функцией Hide My Email в iCloud+.

Обычно Apple позволяет создавать новые адреса-алиасы только вручную, при этом действует ограничение — не более 5 адресов в час. Скрипт берёт эту задачу на себя:
автоматически открывает iCloud+ и ждёт, пока вы авторизуетесь;
создаёт новые адреса по порядку (401, 402, 403 и так далее);
сохраняет все созданные почтовые адреса в отдельный файл для удобного использования;
отслеживает достижение лимита Apple и автоматически ждёт необходимое время;
показывает обратный отсчёт ожидания, чтобы было видно, сколько осталось ждать;
после паузы обновляет страницу и продолжает работу без вашего участия.

Как пользоваться

Запустите скрипт.
Если вы ещё не авторизованы — войдите в свой iCloud (один раз).
Далее скрипт работает автоматически: создаёт почты, записывает их в файл, ждёт час и продолжает.
Скрипт может работать без вашего участия столько времени, сколько потребуется.
Браузер, в котором запущен скрипт, можно сразу свернуть — это не повлияет на его работу.
📌 Идея простая: вы один раз запускаете процесс — и он самостоятельно создаёт для вас новые iCloud-адреса партиями, строго соблюдая лимиты Apple и не требуя ручного контроля.


Donate EVM 0xFa61eb476981FE9aA9A9c8afA26dc81D45Db5917


ГАЙД ПО ССЫЛКЕ 
https://alphacrypto.notion.site/icloud-2ce520faf1fd802ab30ff3b6773e6d4e
